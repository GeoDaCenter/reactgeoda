/* eslint-disable eslint-comments/disable-enable-pair */
/* eslint-disable @next/next/no-img-element */
import React from 'react';

export const IconBoxplot = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon boxplot"
      src="/assets/img/icon-boxplot.svg"
    />
  );
};

export const IconCartogram = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon cartogram"
      src="/assets/img/icon-cartogram.svg"
    />
  );
};

export const IconChatgpt = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon chatgpt"
      src="/assets/img/icon-chatgpt.svg"
    />
  );
};

export const IconChoropleth = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon choropleth"
      src="/assets/img/icon-choropleth.svg"
    />
  );
};

export const IconHistogram = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon histogram"
      src="/assets/img/icon-histogram.svg"
    />
  );
};

export const IconLisa = ({className}) => {
  return (
    <img className={`icon-toolbar ${className}`} alt="Icon lisa" src="/assets/img/icon-lisa.svg" />
  );
};

export const IconMap = ({className}) => {
  return (
    <img className={`icon-toolbar ${className}`} alt="Icon map" src="/assets/img/icon-map.svg" />
  );
};

export const IconOpen = ({className}) => {
  return (
    <img className={`icon-toolbar ${className}`} alt="Icon open" src="/assets/img/icon-open.svg" />
  );
};

export const IconParallel = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon parallel"
      src="/assets/img/icon-parallel.svg"
    />
  );
};

export const IconScatterplot = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon scatterplot"
      src="/assets/img/icon-scatterplot.svg"
    />
  );
};

export const IconTable = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon table"
      src="/assets/img/icon-table.svg"
    />
  );
};

export const IconWeights = ({className}) => {
  return (
    <img
      className={`icon-toolbar ${className}`}
      alt="Icon weights"
      src="/assets/img/icon-weights.svg"
    />
  );
};

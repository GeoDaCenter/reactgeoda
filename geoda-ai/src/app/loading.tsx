export default function Loading() {
  // call async initDuckDB to initialize the database
  // const {loading, error} = useAsync(initDuckDB);
  // You can add any UI inside Loading, including a Skeleton.
  return <></>;
}
